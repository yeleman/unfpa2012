#!/usr/bin/env python
# encoding=utf-8

NOSMSD_HANDLER = 'unfpa_sms.nosms_handler'
NOSMSD_GETTEXT = True
NOSMSD_GETTEXT_LOCALE = 'fr_FR.UTF-8'

NOSMSD_DATABASE = {'type': 'MySQL', 'name': 'unfpasms'}
NOSMSD_DATABASE_OPTIONS = {'user': 'unfpasms', 'passwd': 'unfpasms',
                           'host': 'localhost', 'use_unicode': True}
